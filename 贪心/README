56. 合并区间
https://leetcode-cn.com/problems/merge-intervals/

我的做法：

首先想到排序，按照左端点排序
我们用数组 merged 存储最终的答案。
首先，我们将列表中的区间按照左端点升序排序，再按照右端点升序排序。按顺序依次考虑每个区间：
如果当前区间的左端点在数组 merged 中最后一个区间的右端点之后，那么它们不会重合，我们可以直接将这个区间加入数组 merged 的末尾；
否则，它们重合，我们需要用当前区间的右端点更新数组 merged 中最后一个区间的右端点，将其置为二者的较大值。

方法二：官方做法

只需要按照左端点排序，先把第一组加到list里面

然后便历之后的每一组，和list的最后一组进行比较，如果这一组的start<=list最后一组的end, 说明有重复，更新list最后一组的end值(原最后一组end&当前一组end, 两者中较大的)
如果这一组的start>list最后一组的end, 则可以直接把当前组加到list里面，作为list新的最后一组
